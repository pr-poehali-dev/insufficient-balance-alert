# 🚀 Инструкция по запуску проекта локально

## Что нужно установить

1. **Node.js** версии 18 или выше: https://nodejs.org/
2. **Bun** (опционально, для более быстрой работы): https://bun.sh/

## Пошаговая инструкция

### Шаг 1: Установка зависимостей

```bash
npm install
```

### Шаг 2: Запуск проекта

Вам нужно открыть **2 терминала** и запустить команды:

#### **Терминал 1** - Запуск backend сервера:
```bash
bun run server/index.ts
```

Вы увидите:
```
🚀 Локальный сервер запущен на http://localhost:3001
📊 База данных: /путь/к/server/database.db
✅ Добавлены тестовые мастера
```

#### **Терминал 2** - Запуск frontend:
```bash
npm run dev:client
```

Или просто:
```bash
npm run dev
```

Вы увидите:
```
VITE ready in XXX ms
➜  Local:   http://localhost:5173/
```

### Шаг 3: Откройте браузер

Перейдите по адресу: **http://localhost:5173**

## Страницы приложения

- `/` - Главная страница с онлайн-записью
- `/admin` - Админ-панель для управления заказами
- `/master` - Рабочее место мастера

## Тестовые мастера

При первом запуске автоматически создаются 3 мастера:
- Иван Петров - ул. Ленина, 45
- Сергей Иванов - пр. Мира, 123
- Алексей Смирнов - ул. Гагарина, 78

## База данных

База данных SQLite создаётся автоматически в файле:
```
server/database.db
```

Все данные сохраняются локально на вашем компьютере.

## Возможные проблемы

### Ошибка "Cannot find module"
Убедитесь, что установили все зависимости:
```bash
npm install
```

### Порт 3001 занят
Если порт 3001 занят другим приложением, измените `PORT` в файле `server/index.ts`

### Ошибка подключения к API
Убедитесь, что backend сервер запущен в отдельном терминале

## Структура проекта

```
шиномонтаж/
├── server/              # Backend сервер
│   ├── index.ts        # API endpoints
│   └── database.db     # SQLite база (создаётся автоматически)
├── src/                # Frontend приложение
│   ├── pages/         # Страницы
│   ├── components/    # Компоненты
│   └── lib/
│       └── api.ts     # API клиент
└── package.json       # Зависимости
```

## Что дальше?

После запуска проекта вы можете:
1. Создать тестовую запись через главную страницу
2. Открыть админ-панель и назначить заказ на мастера
3. Открыть панель мастера и выполнить заказ

Все изменения сохраняются в локальной базе данных!
